# Observatory Environment Configuration

# Path to your Kubernetes configuration file
# For k3s: usually /etc/rancher/k3s/k3s.yaml on the server
# For local development: ~/.kube/config
KUBECONFIG=/path/to/your/k3s.yaml

# Backend port (default: 8000)
PORT=8000

# Frontend port (used in docker-compose, default: 3000)
FRONTEND_PORT=3000
